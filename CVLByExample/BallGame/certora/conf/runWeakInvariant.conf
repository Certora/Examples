{
  // files that are part of the scence (eveything Prover can reason about)
  "files": [
    "contracts/BallGame.sol"
  ],
  // the main contract under test and the spec file 
  "verify": "BallGame:certora/specs/BallGame.spec",
  "rule_sanity": "basic"
}
// alternatively, run `certoraRun contracts/BallGame.sol --verify BallGame:certora/specs/BallGame.spec`